# Road to the Record

## とりあえず試したい

```
$ docker compose build --build-arg LAYOUTS_NAME=rttr-gen2
$ docker compose up
```

上記コマンド実行後、 `http://localhost:9090/` で確認可能

## Architecture

- NodeCG
- TypeScript
- Vite
- Svelte
